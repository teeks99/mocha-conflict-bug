{
  "name": "mocha-conflict-bug",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package dispatch:mocha-phantomjs",
    "test-watch": "meteor test --driver-package practicalmeteor:mocha --port 8082"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  }
}
