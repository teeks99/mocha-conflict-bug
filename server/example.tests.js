import { assert } from 'meteor/practicalmeteor:chai';

describe('Server Tasks', () => {
  describe('methods', () => {
    it('example test method', () => {
      var value = 42;
      var expected = 42;
      
      assert.equal(expected, value);
    });
  });
});
    
