import { assert } from 'meteor/practicalmeteor:chai';

describe('Client Tasks', () => {
  describe('methods', () => {
    it('client example test method', () => {
      var value = 42;
      var expected = 42;
      
      assert.equal(expected, value);
    });
  });
});
    
